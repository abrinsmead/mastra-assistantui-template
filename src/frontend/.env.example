# Backend API Configuration
# The frontend connects to the Mastra backend API
# No API keys needed here - they're configured in the backend

# Optional: Chat persistence (sign up on cloud.assistant-ui.com)
# NEXT_PUBLIC_ASSISTANT_BASE_URL=https://api.assistant-ui.com

# Optional: Custom backend URL (if not using default localhost:4111)
# NEXT_PUBLIC_BACKEND_URL=http://localhost:4111